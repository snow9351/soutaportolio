/**
 * Space reserved for the top status strip (clock, battery, settings, etc.).
 * Keep desktop padding, window positions, and overlays aligned with this value.
 */
export const STATUS_STRIP_HEIGHT_PX = 52;
